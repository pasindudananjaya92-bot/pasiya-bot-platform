Honest limits:
- Many sites (Google, GitHub) send X-Frame-Options and appear blank inside iframe — window still stays in OS.
- Live multi-user remote control needs Supabase Realtime (payload share is offline bridge).
- Real Google Drive OAuth needs backend; G: is local virtual mount.
- Daily backup always local; Supabase table os_backups optional.
