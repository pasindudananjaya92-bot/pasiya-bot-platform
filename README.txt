Why not 10,000 options?

Real Windows has thousands of settings; most are OS-kernel features.
In a website we ship a nested menu tree where each leaf DOES something
(open app, API health, external free service, refresh, copy, etc.).

Expand later by adding entries to TREE in the script.
