Overwrite root index.html → Commit → hard refresh

Fixes:
- Keyboard bottom visible (safe-area + scroll)
- Taller keys (54–56px) so one finger = one key
- More gap between keys
- Taller trackpad (~150–160px)
